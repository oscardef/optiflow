# Database Configuration
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# MQTT Broker (your MacBook)
# This is the IP address where mosquitto is running
# To find your IP: ifconfig | grep "inet " | grep -v 127.0.0.1
MQTT_BROKER_HOST=
MQTT_BROKER_PORT=

# API Configuration
NEXT_PUBLIC_API_URL=
